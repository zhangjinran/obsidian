---
date: 2026-03-28
tags:
  - 编程
  - python
  - 语法
---
你现有的 `get_semantic_label_names` 是一个静态方法，它直接遍历 `SemanticLabel` 枚举。如果将来有子类继承它，静态方法仍然会遍历父类的成员，而[[类方法]]则会自动使用子类的成员。

### 静态方法版（当前）

python

@staticmethod
def get_semantic_label_names():
    return [label.name for label in SemanticLabel]  # 硬编码父类
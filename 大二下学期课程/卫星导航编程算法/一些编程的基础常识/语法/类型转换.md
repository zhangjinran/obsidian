---
date: 2026-03-02
tags:
  - 课程
  - 卫星算法
---
# static_cast
### 语法解析



static_cast<目标类型>(表达式)

- **`static_cast`**：C++ 提供的四种强制类型转换之一，主要用于相关类型之间的转换（如数值类型、指针类型、枚举与整数等），在编译时进行类型检查，不会执行运行时检查。
    
- **`<int>`**：目标类型，这里是要转换成的整数类型。
    
- **`(system)`**：要被转换的表达式，即枚举变量或值。
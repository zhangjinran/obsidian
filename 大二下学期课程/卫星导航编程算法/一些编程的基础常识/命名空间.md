---
date: 2026-03-02
tags:
  - 课程
  - 卫星算法
aliases:
  - namespace
---
# 定义
- 就是它定义了一个空间，在这个空间里的每个名字都是需要加一个前缀的。比如std命名空间的里的名字，就应该写为`std::cin`or`std::cout`
# using的用法
使用了using之后，对应的名字就不需要添加前缀了，比如`using namespace std;`那么对应的std：：cin就可以直接写cin就可以了。
---
date: 2026-01-30
tags:
  - 语法
  - python
  - 字符串
aliases:
  - f-string
---
# 作用
把变量插入到字符串中，或控制字符串的显示格式。
# repr()
 `!r` 表示用 `repr()` 的方式格式化变量，也就是显示变量的“官方字符串表示”，通常带引号，便于调试。用于突出想要表达的内容。
 ## 例
```
’’ f"`Trainer(barebones=True, logger={logger!r})` was passed."
```
会突出上文的`logger`
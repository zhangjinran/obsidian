---
date: 2026-02-02
tags:
  - python
  - 语法
  - 数据结构
---
# 切片语法
## 索引切片
### 例1
- `shape` 通常是一个表示数组或矩阵维度的元组或列表。
- `shape[:2]` 会返回 `shape` 中从索引 0 到索引 2（不包含 2）之间的部分，这通常对应于二维数据
- 对应多维数据，比如说三维数据，：代表第一个维度的数据全选；2则代表第二个维度的数据只选择第3个
### 例2
`last_hidden_states[:, 1:]`意思就是第二个维度的数据从第二位一直选择到最后一位。